{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git push*)",
            "command": "scripts/preflight.sh >&2 || { echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"scripts/preflight.sh failed. Fix the failing checks, or re-run it yourself to see the output, before pushing.\"}}'; exit 0; }",
            "timeout": 900,
            "statusMessage": "Running preflight checks before push"
          }
        ]
      }
    ]
  }
}
